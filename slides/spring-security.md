##  Spring Security

- Works both with stateful (cookies) & stateless (OAuth2)
- implemented Out-of-the-box
- SecurityUtils
   - getCurrentLogin()
   - isAuthenticated()
   - isUserInRole()

note:
Just need to choose
Helper class
